<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.salma.salmaalarm.MainActivity">

    <TimePicker
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/timePicker"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="28dp" />

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/spinner"
        android:layout_alignRight="@+id/end_alarm"
        android:layout_alignEnd="@+id/end_alarm"
        android:layout_alignLeft="@+id/start_alarm"
        android:layout_alignStart="@+id/start_alarm" />

    <TextView
        android:text="Did you set the alarm?"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/status"
        android:layout_below="@+id/timePicker"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Trun On"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/start_alarm"
        android:layout_above="@+id/spinner"
        android:layout_toLeftOf="@+id/status"
        android:layout_toStartOf="@+id/status"
        android:layout_marginBottom="11dp" />

    <Button
        android:text="Turn off"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/end_alarm"
        android:layout_alignBaseline="@+id/start_alarm"
        android:layout_alignBottom="@+id/start_alarm"
        android:layout_toRightOf="@+id/status"
        android:layout_toEndOf="@+id/status" />
</RelativeLayout>
